{
  "platform": "docker",
  "app": "device-management-backend",
  "port": 80,
  "healthCheck": {
    "command": "curl --fail http://localhost:80/api || exit 1",
    "interval": 30,
    "timeout": 15,
    "retries": 2,
    "startPeriod": 5
  }
}